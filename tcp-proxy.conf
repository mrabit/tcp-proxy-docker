# TCP/UDP 端口转发配置
# 格式1: listen_port target_address (默认转发 TCP+UDP)
# 格式2: protocol listen_port target_address (protocol: TCP, UDP, BOTH)

# 示例: 同时转发 TCP 和 UDP
1005 192.168.0.7:1005

# 示例: 仅转发 TCP
# TCP 8080 192.168.0.7:80

# 示例: 仅转发 UDP
# UDP 53 8.8.8.8:53

# 示例: IPv6 地址
# BOTH 9000 [::1]:9000
