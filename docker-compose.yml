services:
  tcp_proxy:
    container_name: tcp_proxy
    build: .
    cap_add:
      - NET_BIND_SERVICE
    volumes:
      - ./tcp-proxy.conf:/etc/tcp-proxy.conf
    environment:
      - TZ=Asia/Shanghai
    network_mode: "host"
    restart: always
